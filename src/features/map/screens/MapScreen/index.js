import React from 'react';

import { Map, MapContainer } from './mapElements';

const MapScreen = () => {
  return (
    <>
      <MapContainer>
        <Map />
      </MapContainer>
    </>
  );
};

export default MapScreen;
